--continue
--ignore-errors
# Keep the video file on disk after the post- processing; the video is erased by default
# --keep-video
#
# --no-playlist
#
#--proxy socks5://127.0.0.1:1080

# save path
-o ~/dl/youtube/'%(uploader)s/%(title)s/%(title)s.%(ext)s'
# -o ~/dl/youtube/'%(uploader)s/%(playlist)s/%(playlist_index)s - %(title)s.%(ext)s'

--write-auto-sub

--merge-output-format mp4
