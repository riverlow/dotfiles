# /Users/x/.config/tmuxinator/macos.yml

name: macos
root: ~

windows:
- blocky:
j - blocky serve -c /usr/local/etc/blocky/
- ss:
    root: ~/ss/
    layout: even-horizontal
    panes:
    - bv:
      - ss-local -v -c bv.json -l 12345
    - rack:
      - ss-local -v -c rack.json -l 12346
- new:
  - mux start fg
