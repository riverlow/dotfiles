# /Users/x/.config/tmuxinator/macos.yml

name: bg
root: ~/

windows:
  - tor:
    - tor
  - ss:
    - cd ~/ss/
    - ss-local -v -c dv.json -l 12345
  # - mdbook:
  #   - cd ~/haha/
  #   - mdbook serve
  - caddy:
    - cd ~/dl/data_caddy/
    - caddy start
  - new: 
    - tmuxinator start code
    - tmuxinator start fg
